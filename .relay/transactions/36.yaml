uuid: d89c19b2-3eb4-47b1-912f-6825c61d56c2
projectId: scn-landing-web-gemini
createdAt: '2025-09-12T02:43:18.676Z'
gitCommitMsg: |-
  feat: add animated smoky background to hero section
  - Creates a new `SmokyBackground` component for a reusable, animated
    background effect.
  - Adds `smoky-bg` keyframes and animation utilities to the Tailwind
    configuration for a subtle, drifting motion.
  - Integrates the `SmokyBackground` into the `Hero` component and updates
    its container to properly position and clip the effect.
promptSummary: The user requested an animated smoky background for the hero section.
reasoning:
  - >-
    I will add an animated smoky background to the hero section. To achieve
    this, I'll create a new `SmokyBackground` component, add the necessary
    keyframes and animation utilities to the Tailwind configuration, and then
    integrate this new component into the `Hero` section.
  - >-
    First, I'll create a new file for our background component. This component
    will contain two large, blurred, circular elements with different animations
    to create a slow, smoky, drifting effect.
  - >-
    Next, I'll define the animations in `tailwind.config.cjs`. I'll add a
    `smoky-bg` keyframe set that produces a subtle, non-linear movement, and
    then create two animation utilities that use these keyframes with different
    durations and directions.
  - >-
    Finally, I'll update the `Hero` component to use this new `SmokyBackground`.
    I'll add `relative` and `overflow-hidden` to the section container to
    properly position and clip the background effect.
operations:
  - type: write
    path: src/components/SmokyBackground.tsx
    content: |-
      export const SmokyBackground = () => (
        <div className="pointer-events-none absolute inset-0 -z-10 overflow-hidden">
          <div className="absolute left-1/2 top-1/2 size-[1000px] -translate-x-1/2 -translate-y-1/2">
            <div className="animate-smoky-bg-1 absolute left-0 top-0 h-[600px] w-[600px] rounded-full bg-primary opacity-15 blur-3xl" />
            <div className="animate-smoky-bg-2 absolute bottom-0 right-0 h-[500px] w-[500px] rounded-full bg-secondary opacity-15 blur-3xl" />
          </div>
        </div>
      );
    patchStrategy: replace
  - type: write
    path: tailwind.config.cjs
    content: |+
      --- tailwind.config.cjs
      +++ tailwind.config.cjs
      @@ -79,6 +79,22 @@
                 from: { height: "var(--radix-accordion-content-height)" },
                 to: { height: "0" },
               },
      +        "smoky-bg": {
      +          "0%": {
      +            transform: "translate(0, 0) scale(1)",
      +          },
      +          "25%": {
      +            transform: "translate(20px, -30px) scale(1.1)",
      +          },
      +          "50%": {
      +            transform: "translate(-10px, 10px) scale(1)",
      +          },
      +          "75%": {
      +            transform: "translate(-25px, 20px) scale(1.1)",
      +          },
      +          "100%": {
      +            transform: "translate(0, 0) scale(1)",
      +          },
      +        },
               "fade-in": {
                 "0%": {
                   opacity: "0",
      @@ -124,6 +140,8 @@
             animation: {
               "accordion-down": "accordion-down 0.2s ease-out",
               "accordion-up": "accordion-up 0.2s ease-out",
      +        "smoky-bg-1": "smoky-bg 30s ease-in-out infinite",
      +        "smoky-bg-2": "smoky-bg 25s ease-in-out infinite reverse",
               "fade-in": "fade-in 0.5s ease-out forwards",
               "glow": "glow 4s ease-in-out infinite",
               "slide-in-from-top": "slide-in-from-top 0.5s ease-out forwards",

    patchStrategy: standard-diff
  - type: write
    path: src/components/sections/Hero.tsx
    content: >+
      --- src/components/sections/Hero.tsx

      +++ src/components/sections/Hero.tsx

      @@ -8,11 +8,13 @@
       import { ArrowDown, ArrowRight, Terminal, Zap } from "lucide-react";
       import { CodeBlock } from "@/components/CodeBlock";
       import { heroContent } from "@/content/sections.content";
       import { PLAYGROUND_URL } from "@/lib/constants";
      +import { SmokyBackground } from "@/components/SmokyBackground";
       
       export const Hero = () => (
      -  <section className="grid items-center gap-12 pb-24 pt-12 sm:pb-32
      sm:pt-16 lg:grid-cols-2">

      +  <section className="relative grid items-center gap-12 overflow-hidden
      pb-24 pt-12 sm:pb-32 sm:pt-16 lg:grid-cols-2">

      +    <SmokyBackground />
           <div className="space-y-6 text-center lg:text-left">
             <h1 className="animate-fade-in text-4xl font-extrabold tracking-tighter opacity-0 md:text-6xl">
               <span className="bg-gradient-to-b from-foreground to-foreground/70 bg-clip-text text-transparent">

    patchStrategy: standard-diff
snapshot:
  src/components/SmokyBackground.tsx: null
  tailwind.config.cjs: |-
    /** @type {import('tailwindcss').Config} */
    module.exports = {
      darkMode: ["class"],
      content: [
        './index.html',
        './src/**/*.{js,ts,jsx,tsx}',
      ],
      prefix: "",
      theme: {
        container: {
          center: true,
          padding: "2rem",
          screens: {
            "2xl": "1400px",
          },
        },
        extend: {
          fontFamily: {
            sans: ["-apple-system", "BlinkMacSystemFont", "'Segoe UI'", "Roboto", "Helvetica", "Arial", "sans-serif", "'Apple Color Emoji'", "'Segoe UI Emoji'", "'Segoe UI Symbol'"],
            mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'Monaco', 'Consolas', '"Liberation Mono"', '"Courier New"', 'monospace'],
          },
          colors: {
            border: "hsl(var(--border))",
            input: "hsl(var(--input))",
            ring: "hsl(var(--ring))",
            background: "hsl(var(--background))",
            foreground: "hsl(var(--foreground))",
            primary: {
              DEFAULT: "hsl(var(--primary))",
              foreground: "hsl(var(--primary-foreground))",
            },
            secondary: {
              DEFAULT: "hsl(var(--secondary))",
              foreground: "hsl(var(--secondary-foreground))",
            },
            destructive: {
              DEFAULT: "hsl(var(--destructive))",
              foreground: "hsl(var(--destructive-foreground))",
            },
            muted: {
              DEFAULT: "hsl(var(--muted))",
              foreground: "hsl(var(--muted-foreground))",
            },
            accent: {
              DEFAULT: "hsl(var(--accent))",
              foreground: "hsl(var(--accent-foreground))",
            },
            popover: {
              DEFAULT: "hsl(var(--popover))",
              foreground: "hsl(var(--popover-foreground))",
            },
            card: {
              DEFAULT: "hsl(var(--card))",
              foreground: "hsl(var(--card-foreground))",
            },
            syntax: {
              keyword: "hsl(var(--syntax-keyword))",
              string: "hsl(var(--syntax-string))",
              class: "hsl(var(--syntax-class))",
              number: "hsl(var(--syntax-number))",
              comment: "hsl(var(--syntax-comment))",
            },
          },
          borderRadius: {
            lg: "var(--radius)",
            md: "calc(var(--radius) - 2px)",
            sm: "calc(var(--radius) - 4px)",
          },
          keyframes: {
            "accordion-down": {
              from: { height: "0" },
              to: { height: "var(--radix-accordion-content-height)" },
            },
            "accordion-up": {
              from: { height: "var(--radix-accordion-content-height)" },
              to: { height: "0" },
            },
            "fade-in": {
              "0%": {
                opacity: "0",
                transform: "translateY(10px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
            "glow": {
              "0%, 100%": { "box-shadow": "0 0 10px -5px hsl(var(--primary))" },
              "50%": { "box-shadow": "0 0 15px 0px hsl(var(--primary))" }
            },
            "slide-in-from-top": {
              "0%": {
                opacity: "0",
                transform: "translateY(-20px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
            "slide-in-from-bottom": {
              "0%": {
                opacity: "0",
                transform: "translateY(20px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
            "background-pan": {
              "0%": { backgroundPosition: "0% 50%" },
              "50%": { backgroundPosition: "100% 50%" },
              "100%": { backgroundPosition: "0% 50%" },
            },
          },
          animation: {
            "accordion-down": "accordion-down 0.2s ease-out",
            "accordion-up": "accordion-up 0.2s ease-out",
            "fade-in": "fade-in 0.5s ease-out forwards",
            "glow": "glow 4s ease-in-out infinite",
            "slide-in-from-top": "slide-in-from-top 0.5s ease-out forwards",
            "slide-in-from-bottom": "slide-in-from-bottom 0.5s ease-out forwards",
            "slide-in-bottom-glow": "slide-in-from-bottom 0.5s ease-out forwards, glow 4s ease-in-out 0.5s infinite",
            "background-pan": "background-pan 15s ease-in-out infinite",
          },
        },
      },
      plugins: [require("tailwindcss-animate")],
    }
  src/components/sections/Hero.tsx: |-
    import { Button } from "@/components/ui/button";
    import {
      Card,
      CardContent,
      CardHeader,
      CardTitle,
    } from "@/components/ui/card";
    import { ArrowDown, ArrowRight, Terminal, Zap } from "lucide-react";
    import { CodeBlock } from "@/components/CodeBlock";
    import { heroContent } from "@/content/sections.content";
    import { PLAYGROUND_URL } from "@/lib/constants";

    export const Hero = () => (
      <section className="grid items-center gap-12 pb-24 pt-12 sm:pb-32 sm:pt-16 lg:grid-cols-2">
        <div className="space-y-6 text-center lg:text-left">
          <h1 className="animate-fade-in text-4xl font-extrabold tracking-tighter opacity-0 md:text-6xl">
            <span className="bg-gradient-to-b from-foreground to-foreground/70 bg-clip-text text-transparent">
              {heroContent.title}
            </span>{" "}
            <span className="inline-block whitespace-nowrap rounded-full bg-primary/10 px-4 py-2 align-middle text-3xl font-medium text-primary md:text-5xl">
              <span className="relative top-[-0.05em] text-2xl md:text-4xl">
                &lt;
              </span>
              {heroContent.highlightedTitle}
            </span>
          </h1>
          <p className="mx-auto max-w-2xl animate-fade-in text-lg text-muted-foreground opacity-0 [animation-delay:0.2s] md:text-xl lg:mx-0">
            {heroContent.subtitle}
          </p>
          <div className="flex animate-fade-in flex-col justify-center gap-4 opacity-0 [animation-delay:0.3s] sm:flex-row lg:justify-start">
            <a href="#section-5">
              <Button size="lg">
                {heroContent.getStartedButton} <Terminal className="ml-2 size-4" />
              </Button>
            </a>
            <a href={PLAYGROUND_URL} target="_blank" rel="noopener noreferrer">
              <Button variant="secondary" size="lg">
                {heroContent.playgroundButton} <ArrowRight className="ml-2 size-4" />
              </Button>
            </a>
          </div>
        </div>
        <div className="relative rounded-xl border bg-gradient-to-b from-secondary/30 to-background p-4 lg:p-6">
          <Card className="animate-slide-in-from-top bg-background/50 opacity-0 backdrop-blur-sm [animation-delay:0.5s]">
            <CardHeader className="flex-row items-center justify-between p-4">
              <CardTitle className="text-base font-semibold text-muted-foreground">
                {heroContent.before.title}
              </CardTitle>
              <Zap className="size-5 text-destructive" />
            </CardHeader>
            <CardContent className="p-0">
              <CodeBlock
                lang="typescript"
                className="rounded-t-none border-0 bg-transparent p-4"
                rawString={heroContent.before.rawCode}
              >{heroContent.before.code}</CodeBlock>
            </CardContent>
          </Card>

          <div className="my-6 flex animate-fade-in justify-center opacity-0 [animation-delay:0.7s]">
            <div className="flex size-10 animate-pulse items-center justify-center rounded-full bg-primary text-primary-foreground">
              <ArrowDown className="size-5" />
            </div>
          </div>

          <Card className="animate-slide-in-bottom-glow border-primary/50 bg-background/50 opacity-0 backdrop-blur-sm [animation-delay:0.9s]">
            <CardHeader className="flex-row items-center justify-between p-4">
              <CardTitle className="text-base font-semibold text-muted-foreground">
                {heroContent.after.title}
              </CardTitle>
              <Zap className="size-5 text-primary" />
            </CardHeader>
            <CardContent className="p-0">
              <CodeBlock
                lang="text"
                className="rounded-t-none border-0 bg-transparent p-4"
                rawString={heroContent.after.rawCode}
              >{heroContent.after.code}</CodeBlock>
            </CardContent>
          </Card>
        </div>
      </section>
    );
approved: false
linesAdded: null
linesRemoved: null
linesDifference: null
gitCommittedAt: null
status: undone
id: 36
