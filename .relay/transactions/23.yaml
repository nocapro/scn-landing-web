uuid: f1933cf4-1a6e-41d3-96b0-68131d1b332b
projectId: scn-landing-web-gemini
createdAt: '2025-09-11T14:16:46.003Z'
gitCommitMsg: >-
  feat(hero): enhance glow animation for "after" card

  The existing glow animation was subtle. This change updates the `glow`
  keyframes to use both an outer and an inner `box-shadow`, creating a more
  pronounced pulsing glow effect that makes the border itself appear to be
  glowing. This improves the visual appeal of the "after" code block in the hero
  section.
promptSummary: Update hero section's "after" widget to have an animated glow border.
reasoning:
  - >-
    Based on your request, I'll modify the existing `glow` animation to give the
    "after" widget's border a more pronounced, glowing effect. This involves
    updating the `box-shadow` keyframes in the Tailwind configuration to include
    both an inner and outer glow, which will make the border itself appear to be
    animated and glowing.
  - >-
    I will update the `glow` keyframes in `tailwind.config.cjs` to achieve the
    desired effect.
operations:
  - type: write
    path: tailwind.config.cjs
    content: >+
      --- tailwind.config.cjs

      +++ tailwind.config.cjs

      @@ -79,8 +79,8 @@
                 },
               },
               "glow": {
      -          "0%, 100%": { "box-shadow": "0 0 10px -5px hsl(var(--primary))"
      },

      -          "50%": { "box-shadow": "0 0 15px 0px hsl(var(--primary))" }

      +          "0%, 100%": { "box-shadow": "0 0 4px hsl(var(--primary)), inset
      0 0 4px hsl(var(--primary))" },

      +          "50%": { "box-shadow": "0 0 8px hsl(var(--primary)), inset 0 0
      8px hsl(var(--primary))" }
               },
               "slide-in-from-top": {
                 "0%": {

    patchStrategy: standard-diff
snapshot:
  tailwind.config.cjs: |-
    /** @type {import('tailwindcss').Config} */
    module.exports = {
      darkMode: ["class"],
      content: [
        './index.html',
        './src/**/*.{js,ts,jsx,tsx}',
      ],
      prefix: "",
      theme: {
        container: {
          center: true,
          padding: "2rem",
          screens: {
            "2xl": "1400px",
          },
        },
        extend: {
          fontFamily: {
            sans: ["-apple-system", "BlinkMacSystemFont", "'Segoe UI'", "Roboto", "Helvetica", "Arial", "sans-serif", "'Apple Color Emoji'", "'Segoe UI Emoji'", "'Segoe UI Symbol'"],
            mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'Monaco', 'Consolas', '"Liberation Mono"', '"Courier New"', 'monospace'],
          },
          colors: {
            border: "hsl(var(--border))",
            input: "hsl(var(--input))",
            ring: "hsl(var(--ring))",
            background: "hsl(var(--background))",
            foreground: "hsl(var(--foreground))",
            primary: {
              DEFAULT: "hsl(var(--primary))",
              foreground: "hsl(var(--primary-foreground))",
            },
            secondary: {
              DEFAULT: "hsl(var(--secondary))",
              foreground: "hsl(var(--secondary-foreground))",
            },
            destructive: {
              DEFAULT: "hsl(var(--destructive))",
              foreground: "hsl(var(--destructive-foreground))",
            },
            muted: {
              DEFAULT: "hsl(var(--muted))",
              foreground: "hsl(var(--muted-foreground))",
            },
            accent: {
              DEFAULT: "hsl(var(--accent))",
              foreground: "hsl(var(--accent-foreground))",
            },
            popover: {
              DEFAULT: "hsl(var(--popover))",
              foreground: "hsl(var(--popover-foreground))",
            },
            card: {
              DEFAULT: "hsl(var(--card))",
              foreground: "hsl(var(--card-foreground))",
            },
          },
          borderRadius: {
            lg: "var(--radius)",
            md: "calc(var(--radius) - 2px)",
            sm: "calc(var(--radius) - 4px)",
          },
          keyframes: {
            "accordion-down": {
              from: { height: "0" },
              to: { height: "var(--radix-accordion-content-height)" },
            },
            "accordion-up": {
              from: { height: "var(--radix-accordion-content-height)" },
              to: { height: "0" },
            },
            "fade-in": {
              "0%": {
                opacity: "0",
                transform: "translateY(10px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
            "glow": {
              "0%, 100%": { "box-shadow": "0 0 10px -5px hsl(var(--primary))" },
              "50%": { "box-shadow": "0 0 15px 0px hsl(var(--primary))" }
            },
            "slide-in-from-top": {
              "0%": {
                opacity: "0",
                transform: "translateY(-20px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
            "slide-in-from-bottom": {
              "0%": {
                opacity: "0",
                transform: "translateY(20px)",
              },
              "100%": {
                opacity: "1",
                transform: "translateY(0)",
              },
            },
          },
          animation: {
            "accordion-down": "accordion-down 0.2s ease-out",
            "accordion-up": "accordion-up 0.2s ease-out",
            "fade-in": "fade-in 0.5s ease-out forwards",
            "glow": "glow 4s ease-in-out infinite",
            "slide-in-from-top": "slide-in-from-top 0.5s ease-out forwards",
            "slide-in-from-bottom": "slide-in-from-bottom 0.5s ease-out forwards",
          },
        },
      },
      plugins: [require("tailwindcss-animate")],
    }
approved: false
linesAdded: 2
linesRemoved: 2
linesDifference: 4
gitCommittedAt: null
status: pending
id: 23
